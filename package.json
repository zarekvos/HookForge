{
  "name": "hookforge-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix ui && npm run build --prefix ui",
    "dev": "npm install --prefix ui && npm run dev --prefix ui",
    "preview": "npm install --prefix ui && npm run preview --prefix ui",
    "install-ui": "npm install --prefix ui",
    "build-only": "npm run build --prefix ui"
  }
}