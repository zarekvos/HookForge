{
  "name": "hookforge-monorepo",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm --prefix ui install && npm --prefix ui run build",
    "vercel-build": "npm --prefix ui install && npm --prefix ui run build",
    "dev": "cd ui && npm run dev"
  },
  "workspaces": ["ui"]
}